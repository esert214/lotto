# 🎲 樂透隨機選號器

免安裝，直接用瀏覽器開啟的樂透選號工具。

---

## 檔案說明

| 檔案 | 說明 |
|------|------|
| `lotto.html` | 標準版 |
| `lotto-csv.html` | 標準版 + 歷史紀錄匯出 CSV |
| `data.json` | 冷熱號資料檔（可獨立更新，不需修改 HTML） |
| `fetch-latest.js` | 更新腳本（需要 Node.js） |
| `更新冷熱號.bat` | 👈 雙擊即可執行更新（Windows 專用，免打指令） |

直接用瀏覽器雙擊開啟即可，無需任何安裝。

---

## 支援遊戲

| 遊戲 | 規則 | 圖示顏色 |
|------|------|----------|
| **威力彩** | 第1區 01~38 選6碼 + 第2區 01~08 選1碼 | 紫色 + 金色 |
| **大樂透** | 01~49 選6碼 | 粉紅色 |
| **今彩539** | 01~39 選5碼 | 藍色 |

---

## 功能介紹

### 生成號碼
- **遊戲** — 下拉切換威力彩 / 大樂透 / 今彩539
- **組數** — 一次生成 1~10 組號碼
- **策略** — 四種選號模式
- **排除** — 輸入不想選的號碼（用逗號分隔）
- **生成** — 點擊按鈕隨機產生，號碼自動排序顯示

### 四種選號策略
- **🔄 完全隨機** — 每個號碼機率均等
- **🔥 偏熱門** — 根據歷史出現次數加權，常開號碼機率較高
- **🧊 偏冷門** — 冷門號碼機率較高
- **⚖️ 平衡模式** — 將號碼分為冷、溫、熱三群各取比例混合

### 歷史紀錄
- 每次生成自動儲存至瀏覽器（localStorage）
- 每筆可單獨刪除
- 一鍵清除全部
- 最多保留 100 筆（lotto-csv.html 另可匯出 CSV 檔案）

### 冷熱號參考表
- 切換遊戲時自動更新
- 顯示各遊戲全部歷史統計的熱門 / 冷門號碼
- 威力彩另顯示第2區冷熱號

---

## 冷熱號資料

### 資料來源
歷史開獎資料取自 **HAWO 樂透分析工具**（lotto.hawo.tw），統計至 2026 年 5 月。

| 遊戲 | 統計期數 |
|------|----------|
| 威力彩 | 1,915 期（全部歷史） |
| 大樂透 | 2,139 期（全部歷史） |
| 今彩539 | 5,876 期（全部歷史） |

### 如何更新資料

冷熱號資料存放在 `data.json`，可獨立更新，不需修改 HTML。

**方法一：一鍵自動更新（推薦）**

需要安裝 [Node.js](https://nodejs.org/)。

- **Windows 使用者**：雙擊 `更新冷熱號.bat` 即可
- **其他系統**：在終端機執行 `node fetch-latest.js`

腳本會自動從 HAWO 樂透分析網站擷取三種遊戲的最新冷熱號統計，並寫入 `data.json`。

**方法二：手動編輯**

1. 前往 https://lotto.hawo.tw/ 查看各遊戲最新的冷熱號統計
2. 用記事本之類的文字編輯器打開 `data.json`
3. 修改 `games` 下的 `hot6` / `cold6` / `allFreq` 等欄位
4. 將 `updated` 改為當天日期
5. 儲存檔案後重新整理瀏覽器即可套用

> 即使沒有 `data.json`，HTML 仍可正常運作，會使用內建的備用資料。

---

## 注意事項

- 所有資料僅供參考，不保證中獎
- 請理性購買彩券，量力而為
- 未滿 18 歲不得購買彩券
